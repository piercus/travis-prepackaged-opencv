{
  "name": "travis-prepackaged-opencv",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "semantic-release": "semantic-release"
  },
  "author": "",
  "release": {
    "publish": [
      {
        "path": "@semantic-release/github",
        "assets": [
          {
            "path": "build/*",
            "label": "opencv-built"
          }
        ]
      }
    ]
  },
  "license": "ISC",
  "devDependencies": {
    "semantic-release": "^15.1.7"
  }
}
